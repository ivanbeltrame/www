# www.ivanbeltrame.com
This website is built with [Astro](https://astro.build/). You need to have installed [Node.js](https://nodejs.org/) and [pnpm](https://pnpm.io/) (recommended).
Run `pnpm install` to install dependencies, serve the website with `pnpm dev` and build it with `pnpm build`.